$(document).ready(function () {
  // const time = $('.need_to_be_rendered');


  timeago.format(new Date());

  // time.on('load', function () {
  // time.text = timeago.format(1473245023718);
  // timeago.render(document.querySelectorAll('.need_to_be_rendered'));
  // })

});